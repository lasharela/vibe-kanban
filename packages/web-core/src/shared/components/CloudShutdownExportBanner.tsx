// Self-hosted deployment: the BloopAI Cloud shutdown banner is disabled.
// Signature preserved so existing call sites keep compiling.
interface CloudShutdownExportBannerProps {
  onClick: () => void;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function CloudShutdownExportBanner(_props: CloudShutdownExportBannerProps) {
  return null;
}
